一、 第一層：全域前處理與雙軌組裝 (preprocessing/ 目錄)
此部分負責將原始的 CSV 資料進行基礎清理、欄位型別識別，並組裝成 Scikit-Learn 的 Pipeline。

統一對外介面與 API 窗口：
preprocessing/interface.py
核心職責：前處理模組的 Facade 外觀。提供 preprocess_for_training()（給模型組訓練用，進行雙軌分流與 MI 特徵選擇）及 preprocess_for_inference()（給推論用，進行純轉換，且不刪除任何列並進行特徵強行對齊）。
欄位型別自動路由大腦：
preprocessing/core/router.py
核心職責：AutoRouter 類別。透過缺失率、相異值個數、平均字元長度以及正則表達式，自動將每個欄位分類為 numeric、categorical、high_cardinality、text、datetime 或 dropped。
管線組裝廠：
preprocessing/core/assembler.py
核心職責：PipelineAssembler 類別。根據路由的分組，組裝 ColumnTransformer。
雙軌制實作：包含 Tree 軌道（生肉） 與 DL 軌道（熟肉） 的組裝分支，並自動注入特徵生成分支。
自訂特徵生成與防呆清理器：
preprocessing/processors/feature_generator.py
核心職責：包含多個自定義的 sklearn 轉換器。
RobustDataCleaner：Phase 0 防禦盾牌，拆解 datetime 與強制轉型被弄髒的數值欄。
MIFeatureSelector：基於互資訊（Mutual Information）的特徵選擇器，鎖定 Top-K 個特徵。
GroupedAggregater / PolynomialInteracter / NonLinearScaler：群組聚合統計、多項式乘除交互項、常態化分位數與 Log 偏態矯正。
單一欄位處理器：
preprocessing/processors/category_processor.py
：進行 OrdinalEncoder（樹軌）或 OneHotEncoder（DL軌）處理，並合併稀有類別。
preprocessing/processors/numeric_processor.py
：處理數值型特徵的標準填補與縮放。
preprocessing/processors/text_processor.py
：將自由文本進行 TF-IDF 與 TruncatedSVD 降維。
preprocessing/processors/time_processor.py
：拆解原生日期欄位為多個數值欄位。
多檔案載入與合併：
preprocessing/data_loader.py
：智慧型載入器，支援單一/多個 CSV 檔案載入、記憶體優化壓縮與自動 Join 合併。
二、 第二層：模型/折疊專屬特徵工程 (src/ 目錄)
此部分負責在交叉驗證（CV）中，避免 Target Leakage（標籤洩漏）的 Fold-isolated 特徵工程變換。

模型特徵工程引擎：
src/preprocess.py
FeatureBuilder 類別：在 CV 的每個 Fold 上單獨 fit_transform，實作 10 種模型特徵集（如 raw、signal、pca64、raw_stat、poly2、ts_tabular 等），供 Optuna 鎖定最佳特徵集。
TSFeatureBuilder 類別：時序回歸的因果滯後（Lag）特徵與滾動窗口（Rolling Mean/Std）統計。
三、 與 Pipeline 串接與呼叫的關鍵檔案
全域入口點：
run_pipeline.py
核心串接在 _smart_prepare() 函數，它會載入 preprocessing.interface 並呼叫 preprocess_for_training，取得 X_dict_tr["tree"] 作為後續模型訓練的輸入矩陣。
訓練與交叉驗證器：
src/train.py
在 run_cv() 的交叉驗證 Folds 迴圈中，實例化 FeatureBuilder 來處理該 Fold 的特徵轉換。
超參數優化器：
src/hpo.py
在 TabularHPO.run() 中，預先將所有特徵集（FeatureBuilder）與 Folds 組合進行轉換與快取，消除 Optuna 目標函數中重複的前處理計算，極大地加速了搜尋過程。