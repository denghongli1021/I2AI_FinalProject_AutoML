# 模型訓練架構規格書

**專案名稱**：End-to-End AutoML Platform for Predictive Modeling and Decision Support  
**文件涵蓋範圍**：Feature Extraction → Hyperparameter Optimization → Architecture Search and Ensemble  
**文件語言**：繁體中文  
**參考來源**：Final_Project_for_introAI (2) (2).pdf

---

## 目錄

1. [執行環境說明](#0-執行環境說明)
2. [特徵萃取模組（Feature Extraction）](#1-特徵萃取模組)
3. [超參數優化模組（Hyperparameter Optimization）](#2-超參數優化模組)
4. [架構搜尋與模型集成模組（Architecture Search and Ensemble）](#3-架構搜尋與模型集成模組)
5. [模組間資料流總覽](#4-模組間資料流總覽)
6. [關鍵技術參考](#5-關鍵技術參考)
7. [對照組基準比較（Baseline Benchmark）](#6-對照組基準比較)

---

## 0. 執行環境說明

### 0.1 虛擬環境

本專案統一使用 **`ml_platform`** 虛擬環境執行，以隔離套件版本並確保結果可重現。

**啟動環境（每次執行前需先啟動）：**

```powershell
# Windows PowerShell
conda activate ml_platform
```

或（若使用 venv）：

```powershell
.\.venv\ml_platform\Scripts\Activate.ps1
```

**確認目前環境：**

```powershell
conda info --envs        # 確認 ml_platform 前方出現 *
python --version         # 確認 Python 版本
```

---

### 0.2 主要套件

| 套件 | 用途 |
|---|---|
| `scikit-learn` | 特徵選擇、基礎模型、交叉驗證 |
| `xgboost` | L1 基礎層樹模型 |
| `lightgbm` | L1 基礎層樹模型 |
| `optuna` | TPE 超參數優化 |
| `shap` | 特徵重要性剪枝 |
| `torch` | NAS 神經架構搜尋 |
| `autogluon` | 對照組基準（TabularPredictor） |
| `pandas` / `numpy` | 資料處理 |

**安裝所有依賴：**

```powershell
conda activate ml_platform
pip install scikit-learn xgboost lightgbm optuna shap torch autogluon pandas numpy
```

---

### 0.3 執行入口

| 腳本 | 說明 | 範例指令 |
|---|---|---|
| `run_automl.py` | 自製 AutoML Pipeline | `python run_automl.py --csv openml_cc18_data\22_mfeat-zernike.csv` |
| `run_baseline.py` | AutoGluon 對照組 | `python run_baseline.py --csv openml_cc18_data\22_mfeat-zernike.csv` |

**完整對照執行範例：**

```powershell
conda activate ml_platform
cd "d:\UserData\claude_project\人工智慧project"

# 對照組（AutoGluon）
python run_baseline.py --csv openml_cc18_data\22_mfeat-zernike.csv --time-budget 120

# 自製 Pipeline
python run_automl.py --csv openml_cc18_data\22_mfeat-zernike.csv --trials 20

# 同時執行對比
python run_baseline.py --csv openml_cc18_data\22_mfeat-zernike.csv --compare --trials 20
```

---
## 總目標

希望此模型架構可以訓練在任何的時序與非時序資料集上，訓練集有target或class欄位

## 1. 特徵萃取模組

### 1.1 模組定位與目標

特徵萃取模組是整個 AutoML 流水線的核心環節之一。其主要職責是將前處理後的高維原始資料轉換為精煉且具有語意意義的特徵表示，以顯著提升下游模型的泛化能力與預測準確性。

### 1.2 核心處理步驟

#### 1.2.1 互資訊特徵選擇（Mutual Information Feature Selection）

- **演算法**：以互資訊（Mutual Information, MI）分數作為特徵排序依據。
- **機制**：計算每個特徵與目標變數之間的 MI 值，保留前 *K* 個資訊量最高的特徵，捨棄預測力可忽略的變數。
- **目的**：在進入後續複雜運算之前先行降維，降低雜訊特徵對模型的干擾並壓縮計算成本。

#### 1.2.2 多項式交互特徵生成（Polynomial Interaction Features）

- **機制**：對 MI 篩選後留存的特徵，系統自動枚舉所有兩兩組合，生成對應的交互項（pairwise interaction terms）。
- **目的**：擴充非線性特徵空間，使模型得以捕捉特徵之間的乘法關係與協同效應。

#### 1.2.3 分組聚合統計特徵（Grouped Aggregation Statistics）

- **機制**：針對資料集中含有識別符（identifier）性質的欄位（例如地區代碼、使用者 ID），以該欄位進行群組化後計算統計量，包括均值、中位數、標準差、最大值、最小值等。
- **目的**：將群組層級的統計規律嵌入個別樣本的特徵向量中，強化模型對類別層次結構的理解。

#### 1.2.4 時序特徵工程（Time Series Feature Engineering）

針對時間序列型資料，系統透過以下三類特徵擴展方式捕捉複雜的季節性規律與歷史依存關係：

| 特徵類型 | 說明 |
|---|---|
| **落後特徵（Lag Features）** | 以過去 *t* 個時間步的觀測值作為當前時間步的預測輸入。 |
| **滾動統計量（Rolling Statistics）** | 計算滑動視窗內的均值、標準差、最大值等統計量，捕捉局部趨勢。 |
| **日曆特徵（Calendar Features）** | 萃取時間戳的年、月、日、星期、小時等週期性資訊，並以保持循環性的方式編碼（例如正弦／餘弦轉換），確保 12 月與 1 月的距離接近而非遙遠。 |

### 1.3 神經網路專用特徵處理

#### 1.3.1 可學習嵌入層（Learnable Embedding Layers）

- **適用對象**：類別型與文字型屬性（例如「City」欄位）。
- **機制**：透過可訓練的嵌入矩陣，將離散標籤映射至低維連續向量空間，使神經網路得以學習類別標籤之間的潛在語意關係。

#### 1.3.2 動態數值縮放與非線性轉換（Dynamic Numerical Scaling）

當不同特徵的數值範圍差異過大時，數值較大的特徵容易主導梯度更新，導致訓練緩慢或效能下降。系統採用以下方法對特徵分布進行正規化：

- **Z-score 標準化**：將特徵轉換為零均值、單位標準差。
- **對數縮放（Logarithmic Scaling）**：處理右偏分布，壓縮大數值範圍。
- **分位數轉換（Quantile Transformation）**：將特徵映射至均勻或常態分布，降低極端值的影響。

### 1.4 SHAP 驅動的迭代特徵剪枝（Iterative Feature Pruning via SHAP）

- **工具**：Shapley Additive Explanations（SHAP）。
- **流程**：
  1. 利用預先訓練的模型計算每個特徵的邊際貢獻（Shapley 值）。
  2. 移除重要性分數接近零的特徵變數。
  3. 重新訓練並評估模型準確性。
  4. 重複上述步驟，直至模型準確性達到預設的臨界閾值。
- **目標**：在計算效率與預測效能之間取得最佳平衡，避免過多無效特徵消耗推論資源。

---

## 2. 超參數優化模組

### 2.1 模組定位與目標

超參數優化（Hyperparameter Optimization, HPO）模組緊接特徵萃取之後執行，目的是在有限的計算預算內自動搜尋出最能提升預測準確性與計算效率的模型配置組合。

### 2.2 搜尋空間定義

系統定義以下超參數類型作為搜尋對象：

| 超參數類型 | 搜尋方式 | 範例 |
|---|---|---|
| 學習率（Learning Rate） | 對數尺度（Log Scale） | `[1e-5, 1e-1]` |
| Dropout 比率 | 線性均勻採樣 | `[0.0, 0.5]` |
| 正則化係數（Regularization） | 對數尺度 | L1 / L2 lambda |
| 其他模型特定參數 | 依模型類型定義 | 樹深度、葉節點數等 |

### 2.3 搜尋演算法：樹狀結構 Parzen 估計器（TPE）

- **演算法選擇**：Tree-structured Parzen Estimator（TPE）。
- **選擇理由**：在採樣效率與實作彈性之間取得良好平衡，為現代 AutoML 框架中的主流方法之一。
- **運作流程**：
  1. **初始隨機探索**：隨機生成若干組超參數組合，建立初始評估基線。
  2. **貝葉斯導向搜尋**：以期望改進（Expected Improvement, EI）函數作為引導，優先採樣預測表現優於歷史最佳值的超參數區域。
  3. **迭代精煉**：持續更新代理模型，逐步收斂至高效能配置。

### 2.4 搜尋加速策略

#### 2.4.1 中位數剪枝（Median Pruning）

- **機制**：在訓練過程中監控各試驗（trial）的驗證指標；若某試驗的中間評估結果低於同時間點所有試驗的歷史中位數，則提前終止該試驗。
- **效益**：快速淘汰明顯劣質配置，將計算資源集中分配給潛力較高的候選組合。

#### 2.4.2 多精度技術（Multi-Fidelity Optimization）

- **機制**：透過縮減訓練迭代次數（fidelity）對大量超參數組合進行快速初篩，僅讓通過篩選的候選組合進行完整訓練。
- **效益**：在不損失最終搜尋品質的前提下，大幅降低整體計算開銷。

### 2.5 評估機制

- **交叉驗證**：每個試驗均執行 **5-Fold 交叉驗證**，以驗證集分數（Validation Score）作為目標函數。
- **最終輸出**：搜尋完成後，選取表現最佳的 **前五組超參數配置**，注入後續的訓練流水線。

---

## 3. 架構搜尋與模型集成模組

### 3.1 模組定位與目標

本模組整合神經架構搜尋（Neural Architecture Search, NAS）與樹模型參數注入兩條路徑，並將所有優化後的模型統一納入多層堆疊集成（Multi-Layer Stacking Ensemble）架構，以最大化整體預測準確性。

---

### 3.2 神經架構搜尋（Neural Architecture Search, NAS）

#### 3.2.1 設計理念

對於神經網路類型的模型，系統執行 NAS 讓平台「自行設計」最佳網路結構，避免人工手動調整架構帶來的偏差與低效。

#### 3.2.2 搜尋空間定義

NAS 的搜尋空間涵蓋以下網路設計維度：

| 維度 | 說明 |
|---|---|
| 網路深度（Network Depth） | 層數的選擇範圍 |
| 內部連接模式（Connection Patterns） | 前饋、殘差等連接方式 |
| 注意力機制（Attention Mechanisms） | 是否引入自注意力（Self-Attention）層 |
| 跳躍連接（Skip Connections） | 是否在非相鄰層間建立直連路徑 |

#### 3.2.3 One-Shot 超網路訓練（One-Shot Supernet Training）

One-Shot Supernet 是整個 NAS 流程的核心組件，其訓練方式如下：

1. 構建一個包含所有可能子架構的巨型超網路（Supernet）。
2. 訓練超網路時，所有子架構共享網路權重參數（Weight Sharing）。
3. 此方法消除了逐一訓練數千個獨立小模型的需求，大幅降低計算開銷。

#### 3.2.4 最佳架構選取

超網路訓練完成後，透過以下策略從共享權重中擷取最佳子架構：

- **演化演算法（Evolutionary Algorithms）**：模擬生物進化過程，對候選架構進行交叉與突變操作，迭代篩選高效架構。
- **梯度導向策略（Gradient-Based Strategies）**：以架構選擇參數的梯度訊號引導搜尋方向。

選取過程亦同步考量以下實際部署限制：

- 記憶體上限（Memory Limits）
- 硬體規格限制（Hardware Specifications）
- 推論延遲要求（Latency Requirements）

---

### 3.3 樹模型超參數注入（Tree Model Parameter Injection）

針對梯度提升樹（Gradient Boosting Trees）及隨機森林（Random Forests）等演算法，系統將 HPO 模組產出的 **前五組最佳超參數配置** 透過 `hyperparameters` 字典格式注入至 AutoGluon 訓練池，由 AutoGluon 統一調度執行。

---

### 3.4 多層堆疊集成架構（Multi-Layer Stacking Ensemble）

所有經過優化的模型（神經網路與樹模型）統一整合至以下兩層集成架構，以進一步提升預測效能：

#### 3.4.1 L1 基礎層（L1 Base Layer）

| 項目 | 說明 |
|---|---|
| **組成** | 具最佳架構的神經網路 + 各類超參數調優後的模型 |
| **訓練輸入** | 原始輸入特徵 |
| **訓練策略** | 採用交叉驗證機制進行訓練 |
| **輸出** | 折外預測（Out-of-Fold, OOF）預測值 |

- 每個基礎模型以輸入特徵訓練後，透過 K-Fold 交叉驗證生成 OOF 預測，確保預測結果對於訓練樣本而言屬於「未見過」的資料，降低過擬合風險。

#### 3.4.2 L2 集成層（L2 Ensemble Layer）

| 項目 | 說明 |
|---|---|
| **輸入** | L1 基礎層輸出的 OOF 預測值（作為 Meta-features） |
| **任務** | 學習各基礎模型預測的最佳加權組合 |
| **輸出** | 最終加權集成預測結果 |

- L1 層的 OOF 預測被轉化為 **Meta-features**，輸入至 L2 集成層進行加權融合（Weighted Ensembling），以最大化整體預測準確性。

---

## 4. 模組間資料流總覽

```
原始資料（Raw Data）
        ↓
  [前處理模組 - Data Preprocessing]
        ↓
  [特徵萃取模組 - Feature Extraction]
   ├─ MI 特徵選擇
   ├─ 多項式交互特徵
   ├─ 分組聚合統計
   ├─ 時序特徵工程
   ├─ 嵌入層 / 動態縮放（神經網路路徑）
   └─ SHAP 迭代剪枝
        ↓
  [超參數優化模組 - HPO]
   ├─ TPE 搜尋演算法
   ├─ 中位數剪枝
   ├─ 多精度技術
   └─ 輸出：Top-5 超參數配置
        ↓
  [架構搜尋與集成模組 - NAS & Ensemble]
   ├─ NAS（神經網路）：Supernet → 演化/梯度搜尋 → 最佳架構
   ├─ 樹模型：Top-5 參數注入 AutoGluon
   ├─ L1 Base Layer（OOF 預測）
   └─ L2 Ensemble Layer（加權融合）
        ↓
   最終預測結果 + 可解釋性報告
```

---

## 5. 關鍵技術參考

| 技術 | 方法 / 工具 | 參考文獻 |
|---|---|---|
| 互資訊特徵選擇 | Mutual Information | - |
| 特徵重要性評估 | SHAP（TreeSHAP） | Lundberg et al., NeurIPS 2017 |
| 超參數優化主演算法 | TPE | Bergstra et al., NeurIPS 2011 |
| 多精度優化 | Hyperband | Li et al., JMLR 2018 |
| 集成框架 | AutoGluon（Stack Ensemble） | Erickson et al., arXiv 2020 |
| 神經架構搜尋 | One-Shot NAS + 參數共享 | Pham et al., arXiv 2018 |
| 時序特徵工程 | Lag / Rolling / Calendar | Christ et al., Neurocomputing 2018 |

---

## 6. 對照組基準比較（Baseline Benchmark）

### 6.1 設計目的

為客觀衡量自製 AutoML Pipeline 的實際改進程度，系統提供一套 **對照組比較腳本**（`run_baseline.py`），以相同的資料切割條件同步執行多套標準 AutoML 套件，並輸出統一格式的效能對比表。

此比較機制確保平台的設計決策（特徵工程、HPO、NAS、Stacking Ensemble）對預測準確性帶來的貢獻是可量化、可重現的。

---

### 6.2 對照方法一覽

| 對照方法 | 套件 / 策略 | 安裝指令 | 說明 |
|---|---|---|---|
| **Sklearn 常用模型** | scikit-learn | 內建（必定執行） | RandomForest、ExtraTrees、GBT、LogisticReg、SVM |
| **LazyPredict** | lazypredict | `pip install lazypredict` | 一次跑完所有 sklearn 分類 / 回歸器，取 Top-3 |
| **TPOT** | tpot | `pip install tpot` | 基因演算法自動搜尋 sklearn Pipeline 組合 |
| **AutoGluon** | autogluon | `pip install autogluon` | Stacking Ensemble，工業級強基準 |

> 以上對照方法均未使用本平台的特徵萃取（MI、Poly、GroupAgg、SHAP 剪枝）與 NAS 搜尋流程，因此可視為「純 AutoML 調參」能力的上限。

---

### 6.3 執行方式

**快速對照（僅 sklearn，無需額外安裝）：**
```bash
python run_baseline.py --csv openml_cc18_data/22_mfeat-zernike.csv
```

**完整對照（含 TPOT + AutoGluon，時間上限 120 秒）：**
```bash
python run_baseline.py --csv openml_cc18_data/22_mfeat-zernike.csv --time-budget 120
```

**同時對比自製 Pipeline（完整對照組 + 自製 Pipeline）：**
```bash
python run_baseline.py --csv openml_cc18_data/22_mfeat-zernike.csv --compare --trials 20
```

**跳過耗時套件（僅 sklearn + LazyPredict）：**
```bash
python run_baseline.py --csv openml_cc18_data/22_mfeat-zernike.csv --skip-tpot --skip-ag
```

---

### 6.4 輸出格式說明

腳本執行完畢後輸出統一格式的對比表，欄位依任務類型而異：

**分類任務（Classification）：**

```
==============================================================
Method                     Time(s)    Accuracy    F1-macro
--------------------------------------------------------------
★ CustomPipeline             142.3      0.9850      0.9848
AutoGluon                    120.1      0.9770      0.9765
TPOT                         120.8      0.9640      0.9638
ExtraTrees                     3.2      0.9580      0.9575
RF(sklearn)                    2.8      0.9510      0.9505
GBT(sklearn)                  18.4      0.9490      0.9487
LogisticReg                    0.3      0.8920      0.8915
SVM(RBF)                       4.1      0.9200      0.9196
==============================================================
```

**回歸任務（Regression）：**輸出 RMSE 與 R² 指標，排序方式相同。

---

### 6.5 比較指標定義

| 指標 | 任務 | 計算公式 | 解讀 |
|---|---|---|---|
| **Accuracy** | 分類 | 正確預測數 / 總樣本數 | 越高越好 |
| **F1-macro** | 分類 | 各類別 F1 的不加權均值 | 類別不平衡時比 Accuracy 更具參考價值 |
| **RMSE** | 回歸 | √(Σ(y−ŷ)² / n) | 越低越好，單位與目標變數相同 |
| **R²** | 回歸 | 1 − SS_res / SS_tot | 越接近 1 越好；可解釋變異比例 |

---

### 6.6 改進幅度計算

啟用 `--compare` 旗標後，腳本將自動輸出自製 Pipeline 相對最佳對照組的改進量：

```
自製 Pipeline Accuracy: 0.9850  vs  最佳對照組 AutoGluon: 0.9770  (+0.0080)
```

**改進幅度解讀原則：**
- `Δ > +0.01`：顯著改進，特徵工程或 NAS 帶來實質收益。
- `-0.005 < Δ < +0.01`：持平，可能受限於資料集特性或時間預算。
- `Δ < -0.005`：需檢查特徵生成是否引入雜訊，或 HPO 試驗次數是否過少。

---

### 6.7 與主流水線的差異對照

| 比較維度 | 對照組（標準 AutoML） | 自製 Pipeline |
|---|---|---|
| **特徵工程** | 基本前處理（編碼、缺值填補） | MI 篩選 → Poly 交互 → 分組聚合 → SHAP 剪枝 |
| **模型搜尋** | 套件內建搜尋空間 | TPE + NAS（One-Shot Supernet） |
| **集成方式** | 套件內建集成（Bagging / Stacking） | 客製化 L1+L2 OOF Stacking |
| **時序支援** | 多數套件需額外設定 | 內建 Lag / Rolling / Calendar 特徵 |
| **可解釋性** | 有限 | SHAP 特徵重要性全程追蹤 |
