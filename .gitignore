openml_cc18_data/
Dataset/
ucr_ts_80(時序資料)/
visualization/dataset/
visualization/plots/

# 隊友分支的 git worktree (用 git worktree add 建,各自 checkout 一個 origin/* 分支)
# 不是普通資料夾,不要 commit 進主分支
teammate-branches/
i2ai-team.code-workspace
autogluon/
.claude/

# Auth — 本機環境變數 / SQLite fallback DB,絕對不能 commit
.env
api/auth.db



catboost_info/
I2AI-handoff/

# Node / Tailwind build — node_modules 不進 repo;css/tailwind.css 由 CI 編譯產生,不 commit
node_modules/
css/tailwind.css

# 大 model blob (estimator.pkl) 自動 offload 到這個目錄,可被 MODEL_BLOB_DIR env 覆寫。
# 內容絕對不能 commit (動輒 GB),要備份的話用 rsync / 跑 OS 級備份。
model_blobs/