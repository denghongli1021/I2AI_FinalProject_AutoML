一、 核心視覺化引擎 (visualization/ 目錄)
可解釋性核心類別：
visualization/visualizer.py
核心職責：實作 AutoMLVisualizer 類別，為整個系統的 SHAP 計算與 Plotly 渲染核心。
解釋器路由：自動偵測模型類型，若為樹模型則啟用 TreeExplainer（精確解）；若為非樹模型或集成模型則 Fallback 到 PermutationExplainer，並將樣本數限制為最多 500 筆，背景樣本數 100 筆。
三大圖表生成：
generate_beeswarm_plot()：生成全局特徵重要性水平長條圖（Blues 漸層色調）。
generate_waterfall_plot()：生成單一樣本的局部解釋瀑布圖（紅色加分、藍色減分、綠色總分）。
generate_dependence_plot()：自動選擇變異數最大的欄位進行色彩編碼，生成交互依賴散佈圖。
範例與測試腳本（驗證通用性與邊界路徑）：
visualization/test_generality.py
：IBM HR 員工離職（分類）與房價預測（回歸）的通用性測試。
visualization/test_nn.py
：實作 SklearnWrapper，驗證 PyTorch 神經網路（MLP）的 SHAP 近似解與圖表生成。
visualization/test_timeseries.py
：時序回歸（ETTh1 電力數據）的視覺化測試。
二、 串接與獨立執行腳本 (專案根目錄)
Pipeline 訓練後自動串接點：
run_pipeline.py
 & 
run_pipeline_time.py
關鍵函數：_run_shap_visualization()
在用戶啟用 --viz 旗標訓練完成後，自動從 artifacts/ 載入最佳傳統模型（best_tabular_model.pkl）與其擬合特徵工程引擎（best_tabular_model_fb.pkl）。
對測試集進行特徵變換與對齊。
關鍵函數：_build_shap_col_names()
當特徵工程改變了特徵維度（如 pca64 或 poly2），該函數會將變換後的特徵維度映射/還原成具備可讀語意的名稱（如 PC0、feat_1），以確保圖表上的特徵名稱清晰易懂。
獨立執行可解釋性分析：
generate_shap.py
核心職責：如果您不想重新執行耗時的 Pipeline 訓練，只要 artifacts/ 中有先前訓練留下的模型與特徵引擎，執行此腳本即可直接載入並生成 SHAP 視覺化圖表，是快速調試與展示的最佳工具。