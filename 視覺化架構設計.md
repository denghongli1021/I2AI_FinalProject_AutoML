一、 核心視覺化引擎 (visualization/ 目錄)
可解釋性核心類別：
visualization/visualizer.py
核心職責：實作 AutoMLVisualizer 類別，為整個系統的 SHAP 計算與 Plotly 渲染核心。

解釋器路由（自動偵測模型類型）：
- 樹模型（XGBoost, LightGBM）→ TreeExplainer（精確解，速度快，無需取樣）
- 神經網路、SVM 等非樹模型 → PermutationExplainer（近似解，支援任意模型）

PermutationExplainer 效能控制（非樹模型路徑）：
- 樣本數上限：最多取 500 筆（`_max_samples=500`），視覺化精度已足夠
- 背景樣本數：`min(100, len(X_test))` 筆，用於建立 SHAP 基準線
- max_evals 優化：`max_evals = 2 × n_features + 1`，固定只跑 1 次 permutation。
  預設 max_evals=500 相當於對 132 個特徵跑約 4 輪，實際耗時 ~3.5 小時；
  改為 265（1 輪）後縮短至約 25 分鐘，特徵重要性排名穩定，精度損失極小。

三大圖表生成：
generate_beeswarm_plot()：生成全局特徵重要性水平長條圖，取 |SHAP|均值前 15 名，Blues 漸層色調。
generate_waterfall_plot()：生成單一樣本的局部解釋瀑布圖，顯示前 8 大貢獻特徵（紅色加分、藍色減分、綠色總分）；base_value 自動展平，支援分類模型的二維輸出。
generate_dependence_plot()：自動選擇標準差最大且 unique 值 > 10 的欄位進行色彩編碼，生成目標特徵與 SHAP 值的交互依賴散佈圖（RdYlBu 漸層）。

_get_shap_matrix() 格式統一：
- TreeExplainer（分類）→ shap_values.values 為 3D (samples, features, classes)，取 class 1（最後一維）
- PermutationExplainer → 2D (samples, features)，直接回傳
統一輸出為 2D，供三種圖表函數使用。

範例與測試腳本（驗證通用性與邊界路徑）：
visualization/test_generality.py：IBM HR 員工離職（分類）與房價預測（回歸）的通用性測試。
visualization/test_nn.py：實作 SklearnWrapper，驗證 PyTorch 神經網路（MLP）的 SHAP 近似解與圖表生成。
visualization/test_timeseries.py：時序回歸（ETTh1 電力數據）的視覺化測試。

二、 串接與獨立執行腳本 (專案根目錄)
Pipeline 訓練後自動串接點：
run_pipeline.py & run_pipeline_time.py
啟用 --viz 旗標後，訓練完成時同時觸發兩個視覺化函數：

關鍵函數：_run_shap_visualization()（Tabular SHAP）
自動從 artifacts/ 載入最佳傳統模型（best_tabular_model.pkl）與其擬合特徵工程引擎（best_tabular_model_fb.pkl）。
對測試集進行特徵變換與對齊，輸出至 {artifacts_dir}/shap_plots/。

關鍵函數：_run_dl_shap_visualization()（DL SHAP）
從 best_dl_model.pt 讀取 model_name、arch_params、in_features，呼叫 _build_dl_model() 重建 PyTorch 架構並灌回參數。
內嵌 _Predictor 包裝成 sklearn 相容介面（predict_proba / predict），供 PermutationExplainer 使用。
自動嘗試 4 種 global_cfg 組合（同 generate_shap.py 的 run_dl_viz），找到與 checkpoint in_features 吻合的 FeatureBuilder。
輸出至 {artifacts_dir}/shap_plots_dl/，前綴 {dataset_name}_dl。
若 best_dl_model.pt 不存在（例如使用 --skip-dl 訓練）則自動跳過，不影響 Tabular SHAP。

DL SHAP 速度控制參數（run_pipeline.py CLI）：
--dl-shap-samples：X_test 的取樣上限（預設 200，可設為 50 以控制在 10 分鐘內）

關鍵函數：_build_shap_col_names()
當特徵工程改變了特徵維度（如 pca64 或 poly2），該函數會將變換後的特徵維度映射/還原成具備可讀語意的名稱（如 PC0、feat_1），以確保圖表上的特徵名稱清晰易懂。兩個視覺化函數共用此函數。

獨立執行可解釋性分析：
generate_shap.py
核心職責：如果您不想重新執行耗時的 Pipeline 訓練，只要 artifacts/ 中有先前訓練留下的模型與特徵引擎，執行此腳本即可直接載入並生成 SHAP 視覺化圖表，是快速調試與展示的最佳工具。

DL 模型的 global_cfg 自動偵測（generate_shap.py 的關鍵修正）：
DL checkpoint（.pt）中記錄了訓練時的 in_features（如 132）。若 FeatureBuilder 的 global_cfg 設定不符（例如缺少 use_kmeans=True 導致少 45 個 KMeans 距離特徵），模型收到錯誤維度的輸入會直接崩潰（RuntimeError: shape invalid）。
解決方式：run_dl_viz() 啟動時，依序嘗試四種 global_cfg 組合：
  (use_kmeans=True,  use_kpca=False)
  (use_kmeans=False, use_kpca=False)
  (use_kmeans=True,  use_kpca=True)
  (use_kmeans=False, use_kpca=True)
找到 FeatureBuilder.transform(X[:1]).shape[1] == checkpoint.in_features 的組合即停止，確保特徵維度與訓練時完全吻合。

DL SHAP 速度控制參數（generate_shap.py CLI）：
--dl-test-samples：X_test 的取樣上限（預設 200，建議設為 50 以控制在 25 分鐘內）
--dl-bg：PermutationExplainer 的 background 樣本數（預設 100）
