# 模型訓練架構規格書 v2

**專案名稱**：End-to-End AutoML Platform for Tabular / Signal Classification  
**文件涵蓋範圍**：Data Preprocessing → Feature Engineering → HPO → NAS → Ensemble  
**文件語言**：繁體中文  
**參考來源**：reverence_pipeline.md（AI HW3 最佳提交架構）

---

## 目錄

1. [執行環境說明](#0-執行環境說明)
2. [資料前處理模組（Data Preprocessing）](#1-資料前處理模組)
3. [特徵工程模組（Feature Engineering）](#2-特徵工程模組)
4. [超參數優化模組（Hyperparameter Optimization）](#3-超參數優化模組)
5. [模型庫定義（Model Library）](#4-模型庫定義)
6. [交叉驗證訓練迴圈（Cross-Validation Training Loop）](#5-交叉驗證訓練迴圈)
7. [架構搜尋與模型集成模組（NAS & Ensemble）](#6-架構搜尋與模型集成模組)
8. [模組間資料流總覽](#7-模組間資料流總覽)
9. [關鍵技術參考](#8-關鍵技術參考)
10. [對照組基準比較（Baseline Benchmark）](#9-對照組基準比較)

---

## 設計核心原則

> **「Pipeline 中不人為固定任何超參數」**
>
> 所有影響模型效能的數值型/類別型設定（學習率、正則化係數、Dropout、Label Smoothing、Mixup 強度、特徵集選擇、網路層數、注意力頭數等），皆由 HPO 或 NAS 搜尋演算法在明確定義的搜尋空間內自動決定。文件中出現的數值僅用於描述「搜尋範圍」，非固定值。

---

## 0. 執行環境說明

### 0.1 虛擬環境

本專案統一使用 **`ml_platform`** 虛擬環境執行，以隔離套件版本並確保結果可重現。

**啟動環境：**

```powershell
conda activate ml_platform
```

**確認環境：**

```powershell
conda info --envs        # ml_platform 前方出現 *
python --version         # 確認 Python 版本
```

---

### 0.2 主要套件

| 套件 | 用途 |
|---|---|
| `scikit-learn` | 前處理、傳統模型、交叉驗證框架 |
| `xgboost` | Tabular 基礎模型 |
| `lightgbm` | Tabular 基礎模型 / Meta-Learner |
| `catboost` | Tabular 基礎模型 |
| `optuna` | TPE 超參數優化 |
| `torch` | 深度學習模型（MLP / CNN1D / Transformer）+ NAS |
| `scipy` | Nelder-Mead 權重最佳化 |
| `pandas` / `numpy` | 資料處理 |

**安裝所有依賴：**

```powershell
conda activate ml_platform
pip install scikit-learn xgboost lightgbm catboost optuna torch scipy pandas numpy
```

---

### 0.3 執行入口

| 腳本 | 說明 |
|---|---|
| `run_pipeline.py` | 完整 Pipeline 執行入口（5-Fold CV → Ensemble → Submission） |
| `src/config.py` | 統一管理亂數種子、路徑、裝置偵測（自動切換 CPU / GPU） |

---

## 總目標

針對 **15 類別表格/訊號分類任務**，以全自動化的方式：

1. 避免資料洩漏（Data Leakage）
2. 產生多樣化的特徵表示供不同模型使用
3. 透過 HPO + NAS 自動決定所有超參數與架構
4. 以 OOF Stacking 與 Nelder-Mead Weighted Blending 融合所有模型
5. 輸出最終 Submission CSV

---

## 1. 資料前處理模組

### 1.1 模組定位

`FeatureBuilder` 類別（`src/preprocess.py`）統一管理所有前處理步驟，嚴格遵循「**只在 Training Fold 上 Fit，再 Transform Validation / Test Set**」的原則，杜絕資料洩漏。

### 1.2 前處理流程

```
原始 CSV（train.csv / test.csv）
       ↓
SimpleImputer（以中位數填補缺值）
       ↓
IQR Clipping（以四分位距截斷極端值）
       ↓
特徵工程（見第 2 節）
       ↓
StandardScaler（Z-score 標準化）
```

> 上述步驟中，IQR 的截斷倍數（clip factor）與 StandardScaler 的使用與否，均列為 HPO 搜尋空間的一部分，不人為固定。

### 1.3 資料切割策略

- 採用 `StratifiedKFold(n_splits=5)`，確保每個 Fold 保持原始 15 個類別的分布比例。
- 所有 Fold 使用統一亂數種子（`SEED`），由 `config.py` 集中管理，確保結果可重現。

### 1.4 PyTorch 整合

- 提供客製化 `TabularDataset` 與 `DataLoader`，供深度學習模型批次化訓練。
- Batch Size 列為 HPO 搜尋空間，不人為固定。

---

## 2. 特徵工程模組

### 2.1 模組定位與目標

特徵工程的核心目標是提供**多樣化的特徵表示（feature sets）**，使不同類型的模型（樹模型、MLP、CNN、Transformer）各自使用最適合其歸納偏置的特徵空間。

`feature_set` 選擇本身被納入 HPO 搜尋空間，由演算法在 5-Fold CV 評估下自動決定各模型的最佳特徵組合。

### 2.2 支援的特徵集（Feature Sets）

| Feature Set | 說明 |
|---|---|
| `raw` | 原始 1280 維特徵，不做額外轉換。 |
| `signal` | `raw` 特徵 + **逐列（Row-wise）正規化**後的特徵拼接，強化相對振幅資訊。 |
| `pca64` | 對原始特徵執行 PCA 降至 64 維，捕捉全域線性結構；降維目標維度列為搜尋空間。 |
| `svd64` | 以 TruncatedSVD 降至 64 維，適用於稀疏或大維度場景；目標維度列為搜尋空間。 |
| `raw_stat` | `raw` + **16 維全域統計特徵**（均值、標準差、偏度、峰度等）+ **32 維局部統計特徵**（8 等分段的均值／標準差）。 |
| `raw_stat_fft` | `raw_stat` + **26 維 FFT 頻域特徵**（振幅、各頻段能量占比等），捕捉週期性訊號模式。 |

> 局部統計的分段數（8 段）與 FFT 特徵的頻段設計皆列為搜尋空間，不人為固定。

### 2.3 模型–特徵集搭配原則

各模型類型可用的特徵集由 HPO 在以下**候選空間**內自動選擇：

| 模型類型 | 候選特徵集 |
|---|---|
| 樹模型（LGBM, XGB, CatBoost, RF） | `raw`, `raw_stat`, `raw_stat_fft` |
| 線性模型（LogReg, SVM） | `pca64`, `svd64` |
| MLP | `raw`, `raw_stat`, `raw_stat_fft`, `signal` |
| CNN1D | `raw`, `signal` |
| Transformer | `raw`, `raw_stat_fft`, `signal` |

### 2.4 資料洩漏防護

- PCA / SVD 的 `fit` 僅在 Training Fold 執行，Validation / Test 使用同一轉換器的 `transform`。
- 統計特徵（均值、標準差等）全數以 Training Fold 的分布計算，推論期套用相同統計量。

---

## 3. 超參數優化模組

### 3.1 模組定位與目標

HPO 模組的職責是在有限計算預算內，以貝葉斯搜尋演算法自動決定**所有模型的超參數組合**，以及各模型應使用的特徵集，不允許人工固定任何數值型設定。

### 3.2 搜尋演算法：TPE

- **演算法**：Tree-structured Parzen Estimator（TPE），由 `Optuna` 框架實作。
- **運作流程**：
  1. 初始隨機探索：生成若干組超參數，建立評估基線。
  2. 貝葉斯導向搜尋：以期望改進（Expected Improvement, EI）函數引導採樣，優先搜尋高效能區域。
  3. 迭代精煉：持續更新代理模型，逐步收斂至最佳配置。
- **評估指標**：5-Fold CV 的 **Macro F1 Score**（OOF）。

### 3.3 搜尋加速策略

#### 3.3.1 中位數剪枝（Median Pruning）

- 訓練中途監控 Validation Macro F1；若低於同時間點所有試驗的歷史中位數，提前終止該試驗。

#### 3.3.2 多精度技術（Multi-Fidelity Optimization）

- 先以較少的訓練輪數（Epoch / Rounds）對大量候選快速篩選，通過初篩者再以完整訓練輪數評估。

### 3.4 各模型搜尋空間定義

#### 3.4.1 傳統機器學習模型

| 模型 | 搜尋參數 | 搜尋範圍 |
|---|---|---|
| LGBM | `num_leaves` | `[16, 512]`（log scale） |
| LGBM | `learning_rate` | `[1e-3, 0.3]`（log scale） |
| LGBM | `min_child_samples` | `[5, 100]` |
| LGBM | `subsample` | `[0.5, 1.0]` |
| LGBM | `colsample_bytree` | `[0.5, 1.0]` |
| LGBM | `reg_alpha` | `[1e-8, 10.0]`（log scale） |
| LGBM | `reg_lambda` | `[1e-8, 10.0]`（log scale） |
| XGBoost | `max_depth` | `[3, 12]` |
| XGBoost | `learning_rate` | `[1e-3, 0.3]`（log scale） |
| XGBoost | `subsample` | `[0.5, 1.0]` |
| XGBoost | `colsample_bytree` | `[0.5, 1.0]` |
| XGBoost | `reg_alpha` | `[1e-8, 10.0]`（log scale） |
| XGBoost | `reg_lambda` | `[1e-8, 10.0]`（log scale） |
| CatBoost | `depth` | `[4, 10]` |
| CatBoost | `learning_rate` | `[1e-3, 0.3]`（log scale） |
| CatBoost | `l2_leaf_reg` | `[1e-8, 10.0]`（log scale） |
| RF | `n_estimators` | `[50, 500]` |
| RF | `max_depth` | `[3, 30]` 或 None |
| RF | `min_samples_leaf` | `[1, 20]` |
| LogReg | `C`（正則化強度） | `[1e-4, 100]`（log scale） |
| SVM | `C` | `[1e-3, 100]`（log scale） |
| SVM | `gamma` | `[1e-5, 10]`（log scale） |
| 所有模型 | `feature_set` | 見 2.3 節候選清單（類別型搜尋） |

> **Early Stopping** 的 rounds/epochs 亦列為搜尋空間，不人為固定。

#### 3.4.2 深度學習模型（MLP / CNN1D / Transformer）

以下搜尋空間適用於所有 DL 模型，具體架構參數由各模型的 NAS 搜尋決定（見第 6.2 節）。

| 搜尋參數 | 搜尋範圍 |
|---|---|
| Learning Rate（AdamW） | `[1e-5, 1e-2]`（log scale） |
| Weight Decay（AdamW） | `[1e-6, 1e-2]`（log scale） |
| Batch Size | `[32, 512]`（2 的冪次，類別型） |
| Label Smoothing（CrossEntropyLoss） | `[0.0, 0.2]` |
| Mixup Alpha（Beta 分布參數） | `[0.0, 0.5]`（0.0 = 停用） |
| Mixup 觸發機率（per batch） | `[0.0, 1.0]` |
| CosineAnnealingLR T_max | `[5, 50]`（Epoch 數） |
| `feature_set` | 見 2.3 節候選清單 |

### 3.5 評估機制與輸出

- 每個 Trial 執行完整 5-Fold CV，以 OOF Macro F1 為目標函數。
- 搜尋完成後，各模型保留 **前 Top-K 組超參數配置**（K 值列為執行時參數，建議 3–5）。
- Top-K 配置注入後續訓練迴圈，產出 OOF 與 Test 的預測機率矩陣（`.npy`），供 Ensemble 使用。

---

## 4. 模型庫定義

### 4.1 Tabular 模型（`src/models/tabular.py`）

| 模型 | 類型 | 說明 |
|---|---|---|
| `lgbm` | Gradient Boosting | LightGBM；支援 Early Stopping |
| `xgb` | Gradient Boosting | XGBoost；支援 Early Stopping |
| `catboost` | Gradient Boosting | CatBoost；支援 Early Stopping |
| `rf` | Bagging | Random Forest（sklearn） |
| `logreg` | Linear | Logistic Regression（sklearn） |
| `svm` | Kernel | SVM with RBF Kernel（sklearn） |

### 4.2 深度學習模型

| 模型 | 檔案 | 說明 |
|---|---|---|
| `mlp` | `src/models/mlp.py` | 多層感知機；層數與寬度由 NAS 決定 |
| `cnn1d` | `src/models/cnn1d.py` | 1D ResNet 殘差神經網路；深度與殘差模式由 NAS 決定 |
| `transformer` | `src/models/transformer.py` | 1D Signal Transformer；Patch 大小、頭數、層數由 NAS 決定 |

### 4.3 模型與特徵集配對（HPO 決定）

每個模型（`lgbm`, `xgb`, `catboost`, `rf`, `logreg`, `svm`, `mlp`, `cnn1d`, `transformer`）各自獨立進行一次 HPO 搜尋，搜尋結果同時決定其超參數與所使用的特徵集。

---

## 5. 交叉驗證訓練迴圈

### 5.1 流程總覽（`src/train.py`, `run_pipeline.py`）

每個（模型, 特徵集）組合執行獨立的 5-Fold CV：

```
For each (model_name, feature_set) in HPO 最佳配置:
    For each fold in StratifiedKFold(n_splits=5):
        1. FeatureBuilder.fit(X_train_fold) → transform X_train_fold, X_val_fold, X_test
        2. 訓練模型（含 DL 訓練細節，見 5.2）
        3. 儲存最佳 Checkpoint（by Val Macro F1）
        4. 產出 oof_pred[val_idx] = model.predict_proba(X_val_fold)
        5. 產出 test_pred_fold = model.predict_proba(X_test)
    test_pred = mean(test_pred_fold over 5 folds)
    存為 {model_name}_{feature_set}_oof.npy / _test.npy
```

### 5.2 深度學習訓練細節（所有超參數由 HPO 決定）

| 訓練組件 | 實作 | 搜尋空間 |
|---|---|---|
| 最佳化器 | AdamW | LR、Weight Decay（見 3.4.2） |
| 學習率排程器 | CosineAnnealingLR | T_max（見 3.4.2） |
| 損失函數 | CrossEntropyLoss + Label Smoothing | smoothing 值（見 3.4.2） |
| 資料增強 | Mixup（Beta 分布混合兩樣本） | Alpha、觸發機率（見 3.4.2） |
| 訓練停止 | Validation Macro F1 早停 | Patience 列為搜尋空間 |

> **Mixup 實作說明**：每個 mini-batch 以 HPO 搜尋出的機率觸發；觸發時，從 Beta(alpha, alpha) 採樣混合比例 λ，以 `x̃ = λ·xᵢ + (1-λ)·xⱼ`、`ỹ = λ·yᵢ + (1-λ)·yⱼ` 的方式混合。Alpha 與觸發機率均由 HPO 決定，不人為固定。

### 5.3 Artifact 產出規範

每個 Fold 訓練完成後儲存：

```
artifacts/
├── {model}_{feature_set}_fold{k}.pt        # DL 模型最佳 Checkpoint
├── {model}_{feature_set}_fold{k}.pkl       # 傳統模型 pickle
├── {model}_{feature_set}_oof.npy           # shape: (N_train, 15)
└── {model}_{feature_set}_test.npy          # shape: (N_test, 15)
```

---

## 6. 架構搜尋與模型集成模組

### 6.1 模組定位

本模組整合兩項功能：
1. **NAS**：自動決定深度學習模型的網路架構
2. **Ensemble**：融合所有（傳統 + 深度學習）模型的 OOF 預測，最大化最終 Macro F1 Score

---

### 6.2 神經架構搜尋（NAS）

#### 6.2.1 設計理念

對 MLP、CNN1D、Transformer 三類深度學習模型，系統執行 NAS 自動決定最佳網路結構，避免人工架構設計帶來的偏差，並與 HPO（訓練超參數）共同形成端對端的自動化。

#### 6.2.2 搜尋空間定義

**MLP 搜尋空間：**

| 維度 | 搜尋範圍 |
|---|---|
| 層數 | `[1, 6]` |
| 各層隱藏單元數 | `[64, 1024]`（2 的冪次，類別型） |
| 激活函數 | `{ReLU, GELU, SiLU}`（類別型） |
| 是否使用 BatchNorm | `{True, False}` |
| Dropout 比率 | `[0.0, 0.5]` |

**CNN1D 搜尋空間：**

| 維度 | 搜尋範圍 |
|---|---|
| 殘差塊數量 | `[1, 5]` |
| 各塊 Channel 數 | `[32, 256]`（2 的冪次，類別型） |
| Kernel Size | `{3, 5, 7, 9}`（類別型） |
| 是否使用 Global Average Pooling | `{True, False}` |
| Dropout 比率 | `[0.0, 0.5]` |

**Transformer 搜尋空間：**

| 維度 | 搜尋範圍 |
|---|---|
| Patch Size（訊號切割長度） | `[8, 64]`（2 的冪次，類別型） |
| Encoder 層數（Depth） | `[1, 8]` |
| Multi-Head Attention 頭數 | `{2, 4, 8, 16}`（類別型） |
| Feed-Forward 維度 | `[64, 1024]`（2 的冪次，類別型） |
| Dropout 比率 | `[0.0, 0.5]` |

#### 6.2.3 One-Shot Supernet 訓練

1. 構建包含所有子架構的巨型超網路（Supernet），所有子架構共享權重。
2. 每個訓練 Batch 隨機採樣一組子架構進行前向傳播與參數更新（Weight Sharing）。
3. 此方法無需逐一訓練每個子架構，大幅降低搜尋計算成本。

#### 6.2.4 最佳架構選取：演化演算法

超網路訓練完成後，以演化演算法（隨機初始族群 → 突變 → 截斷選擇）從共享權重中擷取最佳子架構：

1. 隨機初始化一組候選架構作為初始族群。
2. 以超網路共享權重直接推論每個候選架構的 Validation Macro F1（免重新訓練）。
3. 對頂部候選進行隨機突變（改變層數、通道數、激活函數等）。
4. 迭代上述步驟，截斷保留表現最佳者。
5. 輸出最佳架構，以完整訓練超參數進行最終獨立訓練。

> 族群大小、演化迭代輪數、突變幅度，均列為 NAS 執行時參數，不人為固定。

---

### 6.3 模型集成架構（Ensemble）

所有模型（傳統 + 深度學習）訓練完成後，以以下兩種策略進行融合：

#### 6.3.1 [Ensemble A] Nelder-Mead Weighted Blending（加權融合）

- **輸入**：所有模型的 OOF 預測機率矩陣（shape: `[N_train, 15]`）
- **融合方式**：以**幾何平均（Geometric Mean）**融合各模型的預測機率。
- **權重最佳化**：
  - 使用 `scipy.optimize.minimize`（Nelder-Mead 方法）。
  - 目標函數：OOF Macro F1 Score（最大化）。
  - 演算法自動搜尋每個模型的最佳權重，不人為指定任何初始權重偏好。
- **輸出**：`sub_A_blend.csv`

#### 6.3.2 [Ensemble B] Meta-Learner Stacking（堆疊集成）

- **第一層（L1 Base Layer）**：所有模型已於 5-Fold CV 中產出 OOF 預測（`[N_train, 15]`），作為 Meta-features。
- **第二層（L2 Meta-Learner）**：
  - 以所有模型的 OOF 預測機率作為輸入特徵。
  - 再次執行 5-Fold CV，訓練 Meta-Learner（候選：`lgbm` 或 `logreg`；**由 HPO 選擇**）。
  - Meta-Learner 學習各模型在不同類別上的強弱，自動決定最終加權方式。
- **輸出**：`sub_B_stack.csv`

| Ensemble 方法 | 機制 | 自動化程度 |
|---|---|---|
| Weighted Blending | 幾何平均 + Nelder-Mead 權重搜尋 | 全自動（無人為權重） |
| Stacking | OOF Meta-features + HPO 選擇 Meta-Learner | 全自動（Meta-Learner 與其超參數由 HPO 決定） |

---

## 7. 模組間資料流總覽

```
1. 原始資料（train.csv / test.csv）
         │
2. FeatureBuilder 前處理（SimpleImputer → IQR Clipping → StandardScaler）
         │
3. 特徵工程（raw / signal / pca / svd / raw_stat / raw_stat_fft）
     ├──> [HPO 搜尋] 決定各模型最佳特徵集 + 超參數
     │
4. 模型訓練（5-Fold CV）
     ├──> Tabular Models（LGBM, XGB, CatBoost, RF, LogReg, SVM）
     └──> DL Models（CNN1D + Mixup + Label Smoothing, MLP, Transformer）
              ↑ NAS 決定架構；HPO 決定訓練超參數
         │
     產出：oof_preds.npy（shape: N_train × 15）
           test_preds.npy（shape: N_test × 15）
         │
5. Ensemble 融合
     ├──> [Ensemble A] Nelder-Mead Weighted Blending → sub_A_blend.csv
     └──> [Ensemble B] Meta-Learner Stacking（HPO 決定 Meta-Learner）→ sub_B_stack.csv
```

---

## 8. 關鍵技術參考

| 技術 | 方法 / 工具 | 參考文獻 |
|---|---|---|
| 超參數優化主演算法 | TPE | Bergstra et al., NeurIPS 2011 |
| 多精度優化 | Hyperband | Li et al., JMLR 2018 |
| 神經架構搜尋 | One-Shot NAS + Weight Sharing | Pham et al., arXiv 2018 |
| 演化架構搜尋 | Evolutionary Algorithms | Real et al., ICML 2019 |
| 深度學習最佳化器 | AdamW | Loshchilov & Hutter, ICLR 2019 |
| 學習率排程 | CosineAnnealingLR | Loshchilov & Hutter, ICLR 2017 |
| 資料增強 | Mixup | Zhang et al., ICLR 2018 |
| 訊號特徵 | FFT 頻域分析 | - |
| 集成–Blending | Nelder-Mead 最佳化 | Nelder & Mead, Computer Journal 1965 |
| 集成–Stacking | OOF Stacking | Wolpert, Neural Networks 1992 |
| 損失函數 | Label Smoothing | Szegedy et al., CVPR 2016 |

---

## 9. 對照組基準比較（Baseline Benchmark）

### 9.1 設計目的

為客觀衡量本 Pipeline 的改進貢獻，提供對照組比較腳本，以相同 5-Fold CV 條件評估標準方法。

### 9.2 對照方法一覽

| 對照方法 | 套件 / 策略 | 說明 |
|---|---|---|
| **Sklearn 單一模型** | scikit-learn | RandomForest、ExtraTrees、GBT、LogisticReg、SVM（預設參數） |
| **LazyPredict** | lazypredict | 一次跑完所有 sklearn 分類器，取 Top-3 |
| **TPOT** | tpot | 基因演算法搜尋 sklearn Pipeline 組合 |
| **AutoGluon** | autogluon | 工業級 Stacking Ensemble 基準 |

> 對照組均不使用本 Pipeline 的特徵工程（raw_stat、FFT）、NAS 架構搜尋、Mixup 增強，可視為「純 AutoML 調參」能力上限。

### 9.3 評估指標

| 指標 | 說明 |
|---|---|
| **Macro F1 Score** | 各類別 F1 不加權均值，為本任務主要排名指標 |
| **Accuracy** | 輔助指標 |
| **訓練時間（秒）** | 計算效率參考 |

### 9.4 改進幅度計算

```
自製 Pipeline Macro F1: X  vs  最佳對照組: Y  (Δ = X - Y)
```

| Δ 範圍 | 解讀 |
|---|---|
| `Δ > +0.01` | 顯著改進，特徵工程或 NAS 帶來實質收益 |
| `-0.005 < Δ ≤ +0.01` | 持平，可能受限於資料集特性或計算預算 |
| `Δ ≤ -0.005` | 需檢查特徵生成是否引入雜訊，或 HPO 試驗次數是否過少 |

### 9.5 與本 Pipeline 的差異對照

| 比較維度 | 對照組（標準 AutoML） | 本 Pipeline（v2） |
|---|---|---|
| **特徵工程** | 基本前處理（編碼、補值） | raw / signal / pca / svd / raw_stat / raw_stat_fft（HPO 選擇） |
| **超參數搜尋** | 套件內建搜尋空間（部分固定） | TPE 全搜尋（無人為固定值） |
| **模型多樣性** | 視套件而定 | 6 類傳統模型 + 3 類深度學習模型 |
| **深度學習架構** | 無或預設固定架構 | CNN1D ResNet / Transformer（NAS 決定） |
| **資料增強** | 無 | Mixup（Alpha 與機率由 HPO 決定） |
| **損失函數** | Cross-Entropy（無平滑） | Label Smoothing（smoothing 由 HPO 決定） |
| **集成方式** | Bagging / 套件內建 Stacking | Nelder-Mead Blending + Meta-Learner Stacking |
| **集成權重** | 均等或固定 | 全自動（Nelder-Mead / Meta-Learner） |
