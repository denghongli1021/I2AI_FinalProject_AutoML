# 模型訓練架構規格書 v2

**專案名稱**：End-to-End AutoML Platform for Predictive Modeling and Decision Support
**文件涵蓋範圍**：Task Profiling → Data Quality → Feature Extraction → HPO → NAS → Stacking Ensemble → Explainability
**文件語言**：繁體中文
**版本**：v2（2026-05-05）
**參考來源**：`Final_Project_for_introAI (2) (2).pdf`、`doc/technology.md`（8 篇核心論文）

---

## v1 → v2 主要變更摘要

| # | 變更類型 | 內容 | 對應論文／來源 |
|---|---|---|---|
| 1 | **新增模組** | 第 1 章 任務剖析（Task Profiling）—— 自動偵測任務類型、樣本/特徵規模、類別不平衡、時序性，據此分配後續預算與啟用模組 | 工程實務 |
| 2 | **新增模組** | 第 2 章 資料品質（Data Quality with Confident Learning）—— 在特徵萃取前以 cleanlab 偵測標籤雜訊與類別重疊 | Confident Learning（Northcutt 2022） |
| 3 | **強化** | 第 3 章 特徵萃取整合 **tsfresh + FRESH 假設檢定 + Benjamini-Hochberg FDR**，取代 v1 過於簡略的時序處理 | tsfresh（Christ 2018） |
| 4 | **強化** | 第 3 章 SHAP 剪枝細分為 **Tree SHAP / Kernel SHAP / Deep SHAP**，依模型類型路由 | SHAP（Lundberg 2017） |
| 5 | **替換** | 第 4 章 HPO 由「TPE + Median Pruning」升級為 **BOHB（KDE 貝葉斯 + Hyperband 多預算支架）**，保留 TPE 為退化選項 | BOHB（Falkner 2018）+ Hyperband（Li 2018）+ TPE（Bergstra 2011） |
| 6 | **強化** | 第 5 章 NAS 具體化為 **ENAS：DAG 超圖 + LSTM Controller + Policy Gradient + 參數共享**，並加入硬體感知約束 | ENAS（Pham 2018） |
| 7 | **強化** | 第 6 章 集成層引入 **Repeated Bagging** 與 **Ensemble Selection / Pruning**，避免弱模型拖累 L2 | AutoGluon-Tabular（Erickson 2020） |
| 8 | **新增模組** | 第 7 章 全局預算編排器（Budget Orchestrator）—— 跨模組動態分配計算時間 | AutoGluon 自適應時間分配 |
| 9 | **新增模組** | 第 8 章 可複現性與可觀測性（Reproducibility & Observability）—— seed 管理、experiment tracking、特徵 / 模型快取 | 工程實務 |
| 10 | **重構** | 配置驅動：所有模組參數集中於 `config.yaml`，CLI 旗標僅覆寫常用項 | 工程實務 |

---

## 目錄

0. [執行環境說明](#0-執行環境說明)
1. [任務剖析模組（Task Profiling）](#1-任務剖析模組)
2. [資料品質模組（Data Quality with Confident Learning）](#2-資料品質模組)
3. [特徵萃取模組（Feature Extraction）](#3-特徵萃取模組)
4. [超參數優化模組（HPO via BOHB）](#4-超參數優化模組)
5. [神經架構搜尋模組（ENAS）](#5-神經架構搜尋模組)
6. [多層堆疊集成模組（Stacking Ensemble）](#6-多層堆疊集成模組)
7. [全局預算編排器（Budget Orchestrator）](#7-全局預算編排器)
8. [可複現性與可觀測性](#8-可複現性與可觀測性)
9. [模組間資料流總覽](#9-模組間資料流總覽)
10. [關鍵技術參考](#10-關鍵技術參考)
11. [對照組基準比較](#11-對照組基準比較)

---

## 0. 執行環境說明

### 0.1 虛擬環境

統一使用 `ml_platform`：

```powershell
conda activate ml_platform
python --version
```

### 0.2 套件清單（v2 增訂）

| 套件 | 用途 | 是否新增 |
|---|---|---|
| `scikit-learn` | 特徵選擇、基礎模型、CV | |
| `xgboost` / `lightgbm` / `catboost` | L1 樹模型 | catboost 新增 |
| `optuna` | TPE 退化選項 | |
| `hpbandster` | **BOHB 主要 HPO 引擎** | **新增** |
| `cleanlab` | **標籤雜訊偵測** | **新增** |
| `tsfresh` | **時序特徵自動萃取與 FDR 篩選** | **新增** |
| `shap` | Tree / Kernel / Deep SHAP | |
| `torch` | NAS Supernet | |
| `autogluon` | 對照組 + L2 集成器 | |
| `mlflow` 或 `wandb` | **實驗追蹤** | **新增**（擇一） |
| `pyyaml` | 配置驅動 | **新增** |
| `joblib` | 特徵 / 模型快取 | **新增** |

```powershell
pip install scikit-learn xgboost lightgbm catboost optuna hpbandster cleanlab tsfresh shap torch autogluon mlflow pyyaml joblib pandas numpy
```

### 0.3 執行入口

| 腳本 | 說明 |
|---|---|
| `run_automl.py --config configs/default.yaml --csv <path>` | 自製 Pipeline（**配置驅動**） |
| `run_baseline.py --csv <path> --time-budget 120` | AutoGluon / TPOT / sklearn 對照組 |
| `run_compare.py --csv <path>` | 同時跑兩者並輸出對比報表 |

---

## 1. 任務剖析模組

> v2 新增模組。此前所有模組共用同一套參數，無視資料特性，導致小資料用大搜尋空間、不平衡資料用 Accuracy 評估等問題。

### 1.1 模組定位

在資料載入之後、任何訓練動作之前，自動偵測下列特性，輸出 `TaskProfile` 物件供後續模組消費：

| 特性 | 偵測方法 | 影響 |
|---|---|---|
| 任務類型 | target dtype + unique 比例 | 分類 / 回歸 / 多標籤 |
| 樣本規模 | `len(df)` | BOHB `b_min`/`b_max` 設定、是否啟用 NAS |
| 特徵維度 | `df.shape[1]` | MI Top-K、Poly 是否啟用、是否需 PCA |
| 類別不平衡 | 最少類別比例 | 評估指標切換為 F1-macro / PR-AUC、是否 SMOTE |
| 時序性 | 時間戳欄位 + 排序檢查 | 啟用 tsfresh、改用 TimeSeriesSplit |
| 群組性 | 識別符欄位偵測 | 啟用 GroupKFold、群組聚合特徵 |
| 缺失率 | `df.isna().mean()` | 缺值填補策略選擇 |

### 1.2 路由規則

```
if profile.is_time_series:        啟用 tsfresh、TimeSeriesSplit
elif profile.has_group_id:        啟用群組聚合、GroupKFold
elif profile.is_imbalanced:       評估指標 → F1-macro/PR-AUC、L1 加 class_weight
if profile.n_samples < 5000:      停用 NAS（成本過高）、b_max 縮減為 1/3
if profile.n_features > 1000:     先 PCA 至 200 維再進入 MI
```

---

## 2. 資料品質模組

> v2 新增模組。v1 假設輸入標籤皆正確；現實 OpenML / Kaggle 資料集普遍存在 3–10% 標籤雜訊，會直接污染後續所有 OOF 預測。

### 2.1 Confident Learning 流程

依 `doc/technology.md §2`，使用 `cleanlab`：

1. 以一個快速基線模型（LightGBM 預設參數，5-Fold）取得每個樣本的預測機率 $\hat{p}(\tilde{y}=j; x)$。
2. 計算每類別自信閾值 $t_j$，建構自信聯合矩陣 $C_{\tilde{y},y^*}$，估計聯合分佈 $\hat{Q}_{\tilde{y},y^*}$。
3. 套用 `prune_by_noise_rate` 策略，輸出三類樣本標記：

| 標記 | 處置 |
|---|---|
| `clean` | 直接進入下一階段 |
| `suspected_noisy` | 進入「軟訓練」集（在 L1 訓練時降低樣本權重至 0.3） |
| `severe_noisy` | 從訓練集移除（仍保留於診斷報告） |

### 2.2 輸出與安全閥

- 輸出 `data_quality_report.json`：總雜訊比例、各類別混淆機率、可疑樣本索引。
- **安全閥**：若預估雜訊率 > 30%，模組停止剪除並向使用者警示（避免基線模型本身過弱導致誤判）。
- **時序資料豁免**：時序任務跳過 CL（其假設為類別條件雜訊，與時序漂移不相容）。

---

## 3. 特徵萃取模組

### 3.1 互資訊特徵選擇（保留 v1）

MI 排序保留 Top-K，K 由 TaskProfile 決定（小資料 K=20，中資料 K=50，大資料 K=100）。

### 3.2 多項式交互特徵（保留 v1，但加上閘門）

僅在 `n_features < 50` 且 `n_samples > 1000` 時啟用，避免特徵爆炸。

### 3.3 分組聚合統計（保留 v1）

由 TaskProfile 偵測識別符欄位後啟用。

### 3.4 時序特徵工程（v2 強化：tsfresh + FRESH）

依 `doc/technology.md §3`，**取代** v1 手寫的 Lag/Rolling/Calendar：

| 步驟 | 內容 |
|---|---|
| **a. tsfresh 提取** | 預設 `EfficientFCParameters`（約 400 特徵，速度與覆蓋率折衷）；高預算模式切換 `ComprehensiveFCParameters`（794 特徵） |
| **b. 假設檢定篩選** | tsfresh 內建 FRESH 演算法，依目標型態自動選擇 KS / Fisher / Mann-Whitney |
| **c. FDR 控制** | Benjamini-Hochberg 程序，預設 FDR 上限 0.05 |
| **d. 平行化** | 透過 `dask` 在多核 CPU 上分散提取 |
| **e. 補強欄位** | 仍保留日曆特徵的 sin/cos 編碼（tsfresh 不含） |

### 3.5 神經網路專用處理（保留 v1，整合至 NAS 模組）

可學習嵌入層、Z-score / Log / Quantile 縮放，移至第 5 章 NAS Supernet 內部處理（讓 ENAS 控制器一併搜尋縮放策略）。

### 3.6 SHAP 驅動迭代剪枝（v2 強化：SHAP 變體路由）

依 `doc/technology.md §4`，按模型類型路由：

| 模型類型 | SHAP 變體 | 計算複雜度 |
|---|---|---|
| 樹模型（XGB / LGB / CatBoost / RF） | **Tree SHAP** | 多項式時間，精確值 |
| 神經網路 | **Deep SHAP**（DeepLIFT 反向傳播） | 線性近似 |
| 異質集成或無法窺視內部 | **Kernel SHAP**（加權最小二乘） | 模型無關，較慢 |

剪枝迴圈：移除 `|φ_i| < ε` 特徵 → 重訓 → 直到驗證指標下降超過 0.5%（早停閾值）。

---

## 4. 超參數優化模組

> v2 將 v1 的「TPE + Median Pruning」升級為 **BOHB**，理由詳見 `doc/technology.md §6 §7`：BOHB = TPE 的模型引導 + Hyperband 的資源分配，在 6 類問題上一致勝過純 TPE 與純 Hyperband。

### 4.1 搜尋空間定義

延續 v1 的搜尋類型（學習率對數、dropout 線性、L1/L2 對數、樹深 / 葉節點數），但統一改寫為 ConfigSpace 格式以供 BOHB / TPE 共用。

### 4.2 主演算法：BOHB

| 元件 | 設定 |
|---|---|
| **預算單位** | 樹模型：`n_estimators`；神經網路：`epochs`；通用：`subsample_ratio` |
| **預算上下界** | `b_min = b_max / 27`（即 $\eta=3$ 三層支架），`b_max` 由全局預算編排器設定 |
| **削減比例 $\eta$** | 預設 3（BOHB 原論文推薦） |
| **KDE 模型** | 連續：高斯核；類別：Aitchison-Aitken 核 |
| **隨機探索比例 $\rho$** | 1/3（防止過早收斂） |
| **冷啟動門檻 $N_{min}$** | 樣本不足時退化為純 Hyperband 隨機採樣 |
| **平行策略** | 共享觀測資料庫 `D`，優先填最小預算支架 |

### 4.3 退化路徑

- **環境無 `hpbandster`**：自動退化為 Optuna TPE + MedianPruner（v1 行為）。
- **單機無平行**：仍可運作，僅損失加速比。

### 4.4 評估機制

| 任務 | CV 策略 | 指標 |
|---|---|---|
| 一般分類 / 回歸 | 5-Fold StratifiedKFold | Accuracy / RMSE |
| 不平衡分類 | 5-Fold StratifiedKFold | F1-macro 或 PR-AUC |
| 時序 | 5-split TimeSeriesSplit | RMSE / MAE |
| 群組資料 | 5-Fold GroupKFold | 同對應任務 |

最終輸出：**Top-5 配置 + 完整觀測資料庫**（後者供 v2 集成器選擇器再利用）。

---

## 5. 神經架構搜尋模組

> v2 將 v1 模糊的「演化 / 梯度導向」具體化為 **ENAS**（`doc/technology.md §8`）：DAG 參數共享 + LSTM 控制器 + 策略梯度，計算成本較傳統 NAS 降低 1000 倍。

### 5.1 搜尋空間（雙模式）

| 任務 | DAG 結構 | 節點操作集 |
|---|---|---|
| 表格資料 | $B$ 個全連接節點 | Linear / GLU / Residual / Attention / Identity |
| 時序資料 | 遞歸單元 ($B$ 節點) | ReLU / tanh / sigmoid / identity（同 ENAS 原設計） |
| 影像 / CNN（保留擴展） | Conv Cell + Reduction Cell | 3×3 conv / 5×5 conv / dw-sep conv / identity |

每節點選擇：(前驅節點, 操作)；總搜尋空間 $\sim (|\text{ops}| \cdot (B-1)!)$。

### 5.2 控制器與訓練流程

| 元件 | 內容 |
|---|---|
| **控制器** | 單層 LSTM，hidden=64，輸出每步決策（前驅 + 操作） |
| **獎勵** | 子模型在 mini-validation 上的指標（分類：accuracy；回歸：-RMSE） |
| **策略梯度** | REINFORCE，加 baseline 減變異 |
| **共享參數 $\omega$** | DAG 每條邊維護一組權重；訓練時依當前子圖採樣的路徑反向傳播 |
| **交替頻率** | 每 epoch 先更新 $\omega$ 一輪（固定 controller），再用 $\omega$ 評估 controller 採樣的 50 個子圖更新 controller |

### 5.3 部署感知約束

最終子圖選取時，將 `latency / params / memory` 加入獎勵作為硬約束項：

$R' = R - \lambda_1 \cdot \text{params} - \lambda_2 \cdot \text{latency}$

由 TaskProfile 中的目標部署環境（CPU only / GPU / Edge）決定 $\lambda$。

### 5.4 退化路徑

- **無 GPU 或 `n_samples < 5000`**：跳過 NAS，直接使用預設 MLP（隱藏層 [256, 128]，dropout 0.3）。
- **時間預算 < 5 min**：跳過 NAS。

---

## 6. 多層堆疊集成模組

### 6.1 L1 基礎層（v2 強化：Repeated Bagging）

依 `doc/technology.md §1`（AutoGluon-Tabular）引入 Repeated Bagging：

| 設定 | v1 | v2 |
|---|---|---|
| Bagging 次數 | 1 | **預算允許下 2–5 次** |
| OOF 來源 | 單次 K-Fold | **Out-of-Bag 平均** |
| 模型池 | NN + Top-5 樹模型 | + CatBoost、ExtraTrees、KNN（多元化） |

### 6.2 L1 → L2 之間：集成選擇（v2 新增）

並非所有 L1 模型都該進 L2。引入 **Caruana Ensemble Selection**：

1. 以驗證集 OOF 為基準，從空集合開始貪心加入使集成指標提升最多的模型（可重複加入加權）。
2. 重複 50 次有放回抽樣減少過擬合。
3. 輸出每個模型的最終權重；**權重 < 0.02 的模型直接從 L2 輸入移除**。

### 6.3 L2 集成層

| 項目 | 設定 |
|---|---|
| 輸入 | L1 篩選後模型的 OOF + 原始特徵的 Top-20（`use_orig_features=True`） |
| 模型 | LightGBM（Stacker）、線性模型（線性權重退路） |
| Bagging | 2 次 Repeated Bagging |
| 自適應層數 | 若預算充裕且 L2 改善 > 0.5%，加開 L3（最多 3 層） |

### 6.4 預測時排錯

對推論輸入做 schema check，若特徵分佈與訓練時 KS 距離 > 0.2，產生告警但仍輸出預測（標記 `confidence_low`）。

---

## 7. 全局預算編排器

> v2 新增模組。v1 各模組各自為政，常出現「HPO 吃光時間，NAS 沒跑完」的失衡。

### 7.1 預算切分（依 TaskProfile 自適應）

預設總預算 $T$（使用者指定，秒）切分如下：

| 模組 | 表格資料 | 時序資料 | 小資料（< 5k） |
|---|---|---|---|
| Task Profiling + Data Quality | 5% | 5% | 5% |
| Feature Extraction（含 SHAP 剪枝） | 15% | 25%（tsfresh 較重） | 15% |
| HPO（BOHB） | 35% | 35% | 50% |
| NAS（ENAS） | 25% | 15% | 0%（停用） |
| Stacking Ensemble | 15% | 15% | 25% |
| 緩衝 / 持久化 | 5% | 5% | 5% |

### 7.2 動態調整

- 各模組設置「軟截止 + 硬截止」雙檢查點。
- 若某模組提早完成，剩餘時間自動加給下一模組。
- BOHB 若觀測到驗證指標連續 10 個 trial 無提升，提前釋放預算。

---

## 8. 可複現性與可觀測性

> v2 新增模組。學術交付不只看分數，需可重現、可比較。

### 8.1 可複現性

- **單一 seed 入口**：`config.yaml` 中 `seed: 42` 同時控制 `numpy` / `random` / `torch` / `optuna` / `hpbandster`。
- **`PYTHONHASHSEED` 與 cuDNN 確定模式**：在 `run_automl.py` 開頭設定。
- **環境快照**：每次執行輸出 `requirements.lock`（`pip freeze`）。
- **資料指紋**：CSV 的 SHA256 寫入結果報表，避免「同一檔名不同內容」混淆。

### 8.2 可觀測性

| 項目 | 工具 |
|---|---|
| 實驗追蹤 | MLflow（本機 file store） |
| 指標 | 每個模組進入/離開時間、各 Trial 分數、SHAP 剪除特徵清單 |
| 工件 | 最終模型、Top-5 配置 JSON、SHAP summary plot、`data_quality_report.json` |
| 失敗診斷 | 每個模組外層套 try/except，落地完整 traceback 與當下狀態 |

### 8.3 快取

- 特徵萃取結果以 `joblib.Memory` 落盤，key = (CSV 指紋, 配置 hash)。
- 重跑相同資料 + 相同配置時，特徵階段秒級回填。

---

## 9. 模組間資料流總覽

```
原始 CSV
    │
    ▼
[1] Task Profiling ──► TaskProfile 物件（路由依據）
    │
    ▼
[2] Data Quality (cleanlab)
    │   └── data_quality_report.json
    ├── clean / soft / drop 分集
    ▼
[3] Feature Extraction
    ├── MI Top-K
    ├── Poly Interaction（閘門控制）
    ├── Group Aggregation
    ├── tsfresh + FRESH FDR（時序）
    ├── Tree/Kernel/Deep SHAP 迭代剪枝
    │
    ▼
[4] HPO via BOHB ──► Top-5 配置 + 觀測資料庫 D
    │
    ▼
[5] NAS via ENAS ──► 最佳子圖架構（含部署約束）
    │
    ▼
[6] Stacking Ensemble
    ├── L1 Repeated Bagging（NN + 樹模型 × Top-5）
    ├── Ensemble Selection（Caruana）
    ├── L2 Stacker (+ 自適應 L3)
    │
    ▼
最終預測 + SHAP 報告 + MLflow 工件

  ▲                                     ▲
  └───[7] Budget Orchestrator 全程控制──┘
  └───[8] Reproducibility & Observability 全程記錄──┘
```

---

## 10. 關鍵技術參考

| 技術 | 方法 / 工具 | 對應論文 |
|---|---|---|
| 標籤雜訊偵測 | Confident Learning（cleanlab） | Northcutt et al., JAIR 2022 |
| 互資訊特徵選擇 | sklearn `mutual_info_classif/regression` | — |
| 時序特徵自動萃取 | tsfresh + FRESH + Benjamini-Hochberg | Christ et al., Neurocomputing 2018 |
| 特徵重要性 | Tree / Kernel / Deep SHAP | Lundberg & Lee, NeurIPS 2017 |
| HPO 主演算法 | BOHB（KDE 貝葉斯 + Hyperband） | Falkner et al., ICML 2018 |
| HPO 退化選項 | TPE | Bergstra et al., NeurIPS 2011 |
| HPO 資源分配 | Hyperband Bracket | Li et al., JMLR 2018 |
| 神經架構搜尋 | ENAS（DAG + LSTM Controller + Policy Gradient） | Pham et al., ICML 2018 |
| 集成框架 | Repeated Bagging + Multi-Layer Stacking | Erickson et al., arXiv 2020 |
| 集成選擇 | Caruana Ensemble Selection | Caruana et al., ICML 2004 |

---

## 11. 對照組基準比較

### 11.1 設計目的（同 v1）

提供 `run_baseline.py`，以相同切割執行多套標準 AutoML 並輸出統一對比表。v2 增加「**消融組（Ablation）**」以量化每項 v2 強化的邊際貢獻。

### 11.2 對照方法（v2 擴增）

| 對照方法 | 套件 | 與本平台差異 |
|---|---|---|
| Sklearn 常用模型 | scikit-learn | 無特徵工程、無搜尋 |
| LazyPredict | lazypredict | 無搜尋 |
| TPOT | tpot | 基因演算法 Pipeline |
| AutoGluon | autogluon | 工業級基準 |
| **本平台 v1** | 本專案 | TPE + 簡易時序 + 單層 OOF |
| **本平台 v2** | 本專案 | BOHB + tsfresh + ENAS + Repeated Bagging + Ensemble Selection |

### 11.3 消融設計（v2 新增）

啟用 `--ablation` 旗標時，依序關閉以下強化以隔離貢獻：

```
v2 完整版
  ├── -CL（關閉 cleanlab）
  ├── -tsfresh（時序退回 v1 手寫特徵）
  ├── -BOHB（HPO 退回 TPE）
  ├── -ENAS（NAS 退回固定 MLP）
  └── -EnsSelect（L2 直接吃所有 L1 模型）
```

### 11.4 輸出格式

```
==================================================================================
Method                          Time(s)    Accuracy    F1-macro    Δ vs AutoGluon
----------------------------------------------------------------------------------
★ CustomPipeline v2               148.6      0.9883      0.9881      +0.0113
  CustomPipeline v1               142.3      0.9850      0.9848      +0.0080
  AutoGluon                       120.1      0.9770      0.9765       baseline
  TPOT                            120.8      0.9640      0.9638      −0.0130
  ExtraTrees                        3.2      0.9580      0.9575      −0.0190
  Ablation: v2 −BOHB              139.1      0.9844      0.9842      +0.0074
  Ablation: v2 −tsfresh           135.8      0.9851      0.9849      +0.0081
  Ablation: v2 −ENAS              112.4      0.9862      0.9860      +0.0092
  Ablation: v2 −EnsSelect         149.2      0.9858      0.9855      +0.0088
  Ablation: v2 −CL                145.9      0.9871      0.9869      +0.0101
==================================================================================
```

### 11.5 改進幅度解讀

| Δ vs 最佳對照組 | 解讀 |
|---|---|
| > +0.01 | 強化奏效 |
| +0.005 ~ +0.01 | 輕度改進（多為時間 / 計算成本換取） |
| −0.005 ~ +0.005 | 持平（資料集本身難度上限） |
| < −0.005 | 檢查是否 BOHB 預算過小或 cleanlab 誤剪 |

---

**文件結束**
